## Change

In `src/pages/VirtualAdvisor.tsx`:

1. Swap the `Phone` icon (in the header circle) for `Mic` from lucide-react.
2. Update the import accordingly.
3. Add a sentence under the existing subtitle: "Make sure to allow your computer or phone to use your microphone if prompted."

No other files affected.